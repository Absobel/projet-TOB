package simcity;
/*
import com.badlogic.gdx.ApplicationAdapter;
import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.graphics.GL20;
import com.badlogic.gdx.graphics.g2d.BitmapFont;
import com.badlogic.gdx.graphics.g2d.SpriteBatch;

public class affichageScore {
	
	private CityManager manager;
	public affichageScore(CityManager manager) {
		this.manager = manager;
	}
	public afficher(SpriteBatch batch,BitmapFont font) {
		//batch.begin();
        font.draw(batch, "Score: " + cityManager.score, 10, 20);
        font.draw(batch, "Level: " + cityManager.level, 10, 20);
        font.draw(batch, "Credits: " + cityManager.credits, 10, 20);
        font.draw(batch, "habitat: " + cityManager.habitat, 10, 20);
        font.draw(batch, "happiness: " + cityManager.happiness, 10, 20);
        //batch.end();
	}
	
	public updateGame() {
		manager.updateLevel();
		manager.updateScore();
		manager.updateHappiness();
		manager.updateCredits();
		manager.updateHabitat();
	}
}
*/